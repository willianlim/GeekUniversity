#include <stdio.h>
//devinindo uma estrutura
struct pessoa
{
    char nome[50], rua[50];
    int indade, numero;
};


int main()
{
    //declaração de variáveis
    char nome[50], rua[50];
    int idade, numero;

    //declaração de uma variável struct
    struct pessoa p;

    return(0);
}

#include <stdio.h>

int main()
{
    /*Tabém podemos declarar um 
    array de ponteiros*/
    //Forma geral:
    //tipo_dado *nome_array[tamanho];
    int *vet[5];//array de 5 ponteiros

    return (0);
}

#include <stdio.h>

int main()
{
    /*
        Arquivo: coleção de bytes armazenados
    em um dispositivo de armazenamento secundário
    
    - disco rígido
    - CD
    - DVD
    - etc

        Vantagens de se usar arquivos:
    
    - Armazenamento durável
    - Permitem armazenar uma grande quantidade
    de informçãoes.
    - Acesso concorrente aos dados

        Cuidado: a extensão do arquivo não
    define o seu tipo, o que define um 
    arquivo é a maneira como os dados estão
    organizados e as operações usadas por um
    programa para processar (ler ou escrever)
    esse arquivo.
    */

    /*
        Arquivos: biblioteca stdio.h

        A linguagem C usa um tipo especial de
    ponteiro para manipular arquivos.
    */
    //Forma geral:
    FILE *nome_ponteiro;

    /*
        É esse ponteiro qe controla o fluxo de
    leitura e escrita dentro de um arquivo.
    */

    return(0);
}
